# Login/Register frontend

เปิด `index.html` ด้วย static web server (เช่น VS Code Live Server) หลังจากเปิด API ที่ `http://localhost:5000` แล้ว

หาก API อยู่คนละ URL ให้กำหนดจาก DevTools console ก่อนโหลดหน้าใหม่:

```js
localStorage.setItem('apiUrl', 'http://your-server/api')
```
